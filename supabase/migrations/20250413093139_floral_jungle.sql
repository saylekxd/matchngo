/*
  # Fix duplicate migration issue

  This migration is a placeholder to fix the duplicate migration issue.
  It doesn't make any schema changes but ensures the migration version
  is properly registered in the schema_migrations table.
*/

-- This is an empty migration that just ensures the version is registered
SELECT 1;